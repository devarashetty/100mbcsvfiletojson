function sorting(array){
	var arrayone = array.slice(0,array.length/2)
	var arraytwo = array.slice(array.length/2,array.length)	
	var mergeArray = merge(arrayone,arraytwo,array.length);
}


merge(arr1,arr2,n){
	var result = [];
	var i = j =0;
	while(i <= arr1.length && j<= arr2.length){

	}
	while(i > arr1.length )
}
